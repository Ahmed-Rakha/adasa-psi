import React from "react";

import classes from "./Blog.module.css";
export default function Blog() {
  return (
    <section id="blog" className={`${classes["blog-section"]}`}>
      hello from blog
    </section>
  );
}
